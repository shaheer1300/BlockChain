[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "consensus-testbed"
version = "0.1.0"
description = "Discrete-event blockchain consensus simulation testbed."
requires-python = ">=3.11"
